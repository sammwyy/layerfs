# Code style

Comments: 1-2 lines max, only when the WHY is genuinely non-obvious.
Never a comment block longer than the code it documents. Default to no
comment at all. This applies to scripts (`.sh`) too, not just Rust.

No references to ROADMAP.md section numbers or milestone names from
source code comments — the ROADMAP is documentation, not something code
should cite.
